/*
Read the location entered on the web page
*/
/*
function readLocation(event) {
    event.preventDefault();

    // check what date was put into the form field
    let enterLocation = document.getElementById('location').value
    console.log(enterLocation);
    let enterLength = enterLocation.length;
    console.log(enterLength);
    //if (length > 0 && length < 51) {
      //  const storeLocation = postData('/add')

        //else {
          //  alert (alert('Please enter between 1 and 50 characters!');
      //  }
}
    //let enterlenDate = enterDate.length;
    //console.log(enterLength);

    //Client.checkForLocation(enterLocation);

export { readLocation };    
*/
